checks for core.yt_api:
  - missing_count("video_ID") = 0
  - duplicate_count("video_ID") = 0

  - likes_count_greater_than_vid_views = 0:
      name: Check if likes count is greater than video views
      likes_count_greater_than_vid_views_query: |
        SELECT COUNT(*)
        FROM core.yt_api
        WHERE "Likes Count" > "Video Views"

  - comments_count_greater_than_vid_views = 0:
      name: Check if Comments count is greater than video views
      comments_count_greater_than_vid_views_query: |
        SELECT COUNT(*)
        FROM core.yt_api
        WHERE "Comments_Count" > "Video Views"
    
    